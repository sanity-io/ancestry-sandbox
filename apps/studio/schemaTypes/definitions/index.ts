import { button } from "./button";
import { customUrl } from "./custom-url";
import { pageBuilder } from "./pagebuilder";
import { richText } from "./rich-text";
import fragmentReference from "./fragment-reference";

export const definitions = [customUrl, richText, button, pageBuilder, fragmentReference];
